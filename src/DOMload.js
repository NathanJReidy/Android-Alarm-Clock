import { allAlarms } from './logic'

function loadAlarms() {
    allAlarms.forEach((alarm, index) => {

        // Both of the below are included within a main flex div 
        
        // Create top card

        // Create extended, bottom card, but set .style.display to 'none' on creation



    })

}
