let allAlarms = [];

function createAlarm(hours, minutes, period) {
    allAlarms.push({
        hours,
        minutes,
        period,
        active: true,
    })
}


// Function to update alarm object/array with new time


// 



export { allAlarms } 